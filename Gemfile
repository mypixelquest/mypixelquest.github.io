source "https://rubygems.org"

# Use GitHub Pages default builder so local matches production.
gem "github-pages", group: :jekyll_plugins

# Needed for local development on Ruby 3+.
group :development do
  gem "webrick", "~> 1.8"
end
